<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.prj.mapper.RsvInputMapper">
  <select id="selectRoomCharge" resultType="int" parameterType="String">
    SELECT CHARGE
    FROM ROOM_INFO
    WHERE ROOM_NAME=#{ ROOM_NAME }
  </select>
  
  
  <insert id="insertReservation" parameterType="kr.co.prj.vo.ReservationVO">
  INSERT INTO RESERVATION(USER_ID,USE_DATE,NAME,PHONE,EMAIL,	CHARGE,	RESERVATION_DATE, PAY_STATUS)
  VALUES(#{ id }, #{ year }||'-'||#{ month }||'-'||#{ day } , #{rsv_person}, #{ phone1}||'-'||#{ phone2}||'-'||#{phone3}, #{ email }, #{totalCharge}, SYSDATE, 'N' )	
  
  
  
  
  
  </insert>
</mapper>